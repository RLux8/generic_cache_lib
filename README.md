# stmi_lib
A VHDL 2008 library for some simple, yet very configurable caches.

Currently this library provides:
* A direct-mapped write-back cache



## Memory Interface ##
All caches of this library use the same memory interface:

### Fill interface ###
### Writeback interface ###